/**
 * Demetrius Portal System Configuration Constants
 * Centralizes operational parameters to eliminate environment configuration drift.
 */
export const SYSTEM_CONFIG = {
  // The current active foundational model deployed in our AWS Bedrock pipeline regions
  BEDROCK_MODEL_ID: "anthropic.claude-3-haiku-20240307-v1:0"
};
